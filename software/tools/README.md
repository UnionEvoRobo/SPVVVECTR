# tools/

Standalone helpers with no dependency on the rest of the codebase.

- `RSSI_Check.py` — scans for the struts and reports BLE signal strength.
  Useful for diagnosing dropouts and checking antenna placement.
